<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
    <style type="text/css">
        main {
            background-color: transparent;
            text-align: center;
            position: absolute;
            margin: auto;
            width: 100%;
            top: 40%;
        }
        .btn {
            font-family: cursive;
            margin: 0 20px;
            width: 200px;
            height: 100px;
            font-size: 30px;
            letter-spacing: 2px;
            background-color: #29a3a3;
            color: #fff;
            border: 2px solid #fff;
            border-radius: 5px;
            box-shadow: 0 0 20px 1px #248f8f;
        }
        body {
            background-repeat: no-repeat;
            background-size: cover;
            overflow: hidden;
            width: 100%;
            height: 100%;
            position: absolute;
        }
        h1 {
            width: 15%;
            background-color: #fff;
            opacity: 0.6;
            text-shadow: 3px 3px 5px #000;
            color: #008080;
            font-size: 700%;
            text-align: center;
            margin: auto;
            padding: 2px;
            border-radius: 10px;
        }
    </style>
</head>
<body>

    <header>
        <% include ../partials/header %>
    </header>
    <main>
        
<!-- Meditation:
        -plays music based on user's current mood
        -a button to allow user to start meditation
        -have user pick set time to meditate for(1 min, 5 min, 10 min, nonstop) -->
        <!-- .js will use ajax's POST method -->
        <form id="delaySubmit" method="POST" action="/meditation/amount-meditated">
            <input type="button" class="btn" name="amount" value="1:00"></input>
            <input type="button" class="btn" name="amount" value="5:00"></input>
            <input type="button" class="btn" name="amount" value="10:00"></input>
            <h1 id="timer">
                <span id="minutes"></span>:
                <span id="seconds"></span>
            </h1>
        </form>
        <!-- <button class="btn">Non-stop</button> -->
        
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>
    
        <% include ../partials/script %>

        <script src="/javascript/meditationJax.js"></script>
</body>
</html>


